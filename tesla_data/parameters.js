//APIurl = "YOUR_API_URL" // hardcode the API url

//show_battery_percentage = true; // show the battery percentage above the battery bar
//show_range = true; // show the estimated range above the battery bar
//show_range_est = true; // show range estimated instead of the car's range estimate
//show_data_age = false; // show how stale the data is
//custom_theme = ""; // if you want to load a theme (some available themes are "3d")

//debug_data = ""; // this will force the widget to pull data from iCloud json files (put sample JSON in the themes directory)

//debug_size = "small"; // which size should the widget try to run as when run through Scriptable. (small, medium, large)